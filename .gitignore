# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# dependencies
node_modules/

# build output
dist/
apps/web/dist/

# env / secrets
.env
.env.local
apps/*/.env

# local file storage (dev only — STORAGE_DRIVER=local)
apps/api/storage/

# logs
*.log
npm-debug.log*

# build info
*.tsbuildinfo
